@model PagedList.IPagedList<Projecten2Groep7.ViewModels.ProductViewModel>

@{
    ViewBag.Title = "Catalogus";
}

<h2>Catalogus</h2>
@using (Html.BeginForm())
{
    <div class="form-group has-feedback has-feedback-left">
        <i class="form-control-feedback glyphicon glyphicon-search"></i>
        @Html.TextBox("SearchString", ViewBag.CurrentFilter as string, new {@class = "form-control searchBar", @placeholder = "Zoeken", @id = "zoekenAjax"})

    </div>
    
   
}
@using(Html.BeginForm())
{
    <h4>Doelgroepen:</h4>
    for (int item = 0; item < ViewBag.Doelgroepen.Length; item++)
    {
        @*<div class="checkbox-inline">
            <label><input class="checkboxJS" type="checkbox" id="@item" value="@ViewBag.Doelgroepen[item].Naam" name="checkbox">@ViewBag.Doelgroepen[item].Naam</label>
        </div>*@
        <div class="radio-inline">
            <input class="checkboxJS" type="radio" name="doelgroep" value="@ViewBag.Doelgroepen[item].Naam" /> @ViewBag.Doelgroepen[item].Naam
        </div>

    }<br/>
    <h4>Leergebieden:</h4>
    for (int item = 0; item < ViewBag.Leergebieden.Length; item++)
    {
        @*<div class="checkbox-inline">
                <label><input class="checkboxJS" type="checkbox" id="@item" value="@ViewBag.Doelgroepen[item].Naam" name="checkbox">@ViewBag.Doelgroepen[item].Naam</label>
            </div>*@
        <div class="radio-inline">
            <input class="checkboxJS" type="radio" name="leergebied" value="@ViewBag.Leergebieden[item].Naam" /> @ViewBag.Leergebieden[item].Naam
        </div>

    }




}
 
<p></p>
<div id="producten">
    @Html.Partial("Producten", Model)
    @*@Html.Partial("GeenZoekResultaten",Model)*@
</div>

@section Scripts{
    <script src="~/Scripts/ajax.js"></script>
}